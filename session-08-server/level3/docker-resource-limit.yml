# [Session 08 - Level 3] Docker 리소스 제한 설정
#
# docker-compose.yml에 추가하여 컨테이너의 리소스 사용량을 제한합니다.

services:
  app:
    image: backend-practice:latest
    container_name: grit-app

    # ============================================
    # TODO 1: CPU 제한
    # ============================================
    # cpus: "1.0"  # 최대 1개 CPU 코어 사용

    # ============================================
    # TODO 2: 메모리 제한
    # ============================================
    # mem_limit: 512m      # 최대 512MB 메모리

    # ============================================
    # TODO 3: 결합 예시
    # ============================================
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

# 💡 테스트:
# docker compose up -d --build app
# docker stats grit-app
