# =============================================================================
# AI와 함께 배우는 백엔드 실전 가이드 - 환경 변수 설정
# =============================================================================
# 사용법: 이 파일을 .env로 복사한 후 필요한 값을 수정하세요.
#   cp .env.example .env
# =============================================================================

# --- MySQL ---
MYSQL_ROOT_PASSWORD=root1234

# --- RabbitMQ ---
RABBITMQ_USER=guest
RABBITMQ_PASS=guest

# --- Grafana ---
GRAFANA_USER=admin
GRAFANA_PASS=admin

# --- JWT (세션 07 보안 실습) ---
JWT_SECRET=grit-moments-jwt-secret-key-for-study-only
JWT_EXPIRATION_MS=3600000
