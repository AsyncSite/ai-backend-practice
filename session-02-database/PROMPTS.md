# [Session 02] AI 활용 프롬프트 -- 데이터베이스

## 1단계: 이해

```
B-Tree 인덱스가 어떻게 검색을 빠르게 하는지 시각적으로 설명해줘.
10만 건의 데이터에서 인덱스 없이 검색할 때와 B-Tree 인덱스로 검색할 때의
비교 횟수를 구체적 숫자로 알려줘.
```

```
JPA의 N+1 문제가 뭔지, 왜 발생하는지, 해결 방법 3가지를 코드와 함께 알려줘.
(JOIN FETCH, @EntityGraph, Batch Size)
```

## 2단계: 적용

```
이 EXPLAIN 결과를 분석하고 개선 방법을 제안해줘:
[EXPLAIN 결과를 붙여넣기]
```

```
이커머스 서비스에서 자주 사용할 쿼리 5개를 예상하고,
각 쿼리에 필요한 인덱스를 추천해줘.
테이블: users, restaurants, menus, orders, order_items
```

## 3단계: 검증

```
AI가 추천한 인덱스가 쓰기(INSERT/UPDATE) 성능에 미치는 영향은?
인덱스가 5개인 테이블에 INSERT할 때 vs 인덱스가 1개일 때
성능 차이가 얼마나 날까?
```

```
[AI가 틀릴 수 있는 포인트]
AI는 실제 데이터 분포를 모르므로 실행 계획 예측이 부정확할 수 있습니다.
반드시 직접 EXPLAIN을 실행하여 확인하세요.
```
