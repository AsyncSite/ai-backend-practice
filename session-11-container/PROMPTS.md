# [Session 11] AI 활용 프롬프트 -- 컨테이너/배포

## 1단계: 이해

```
Docker의 레이어 캐싱이 빌드 속도에 미치는 영향을 설명해줘.
Dockerfile에서 COPY 순서를 바꾸면 왜 캐시가 무효화되는지 알려줘.
```

## 2단계: 적용

```
이 프로젝트에 맞는 GitHub Actions workflow 파일을 만들어줘.
Spring Boot + Gradle 프로젝트에서:
1. PR 시: 테스트 실행
2. main 병합 시: Docker 이미지 빌드 + 푸시
```

## 3단계: 검증

```
이 Dockerfile에서 Gradle 의존성 파일을 먼저 복사하는 이유는?
순서를 바꾸면 빌드 시간이 얼마나 달라지는지 설명해줘.
```
