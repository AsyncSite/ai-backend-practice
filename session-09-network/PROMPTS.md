# [Session 09] AI 활용 프롬프트 -- 네트워크

## 1단계: 이해

```
TCP 3-way handshake의 동작 과정을 단계별로 설명해줘.
왜 3번의 교환이 필요한지, 2번이면 안 되는 이유를 알려줘.
```

```
HTTP/1.1, HTTP/2, HTTP/3의 차이를 성능 관점에서 비교해줘.
각각이 해결하려는 문제가 뭔지 설명해줘.
```

## 2단계: 적용

```
curl -v 출력에서 각 줄이 뭘 의미하는지 설명해줘:
[curl -v 출력을 붙여넣기]
```

```
Java의 ServerSocket으로 HTTP 요청을 파싱하는 코드를 보여줘.
GET 요청을 받아서 "Hello, World!" JSON을 응답하는 수준이면 돼.
```

## 3단계: 검증

```
HTTP/1.1의 Keep-Alive가 왜 필요한지,
매 요청마다 TCP 연결을 새로 맺으면 얼마나 느려지는지
구체적인 수치로 설명해줘.
```
